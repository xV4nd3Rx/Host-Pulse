# Host-Pulse
Host Pulse - a fast, lightweight domain liveness probe for pentesters. Checks large domain lists, detects live hosts (including forbidden/error codes), uses random User-Agents, smart delays, and quick DNS timeouts to skip dead targets efficiently. Outputs clean TXT and CSV reports.
